const soma      = (a,b) => a + b
const subtracao = (a,b) => a - b
const multiplicacao = (a,b) => a * b
const divisao = (a,b) => a / b


const opercao = (a,b) => {
    return ` Soma ${a} e ${b} = ${soma(a,b)}
    Soma ${a} e ${b} = ${soma(a,b)}
    Soma ${a} e ${b} = ${soma(a,b)}
    Soma ${a} e ${b} = ${soma(a,b)}`
}